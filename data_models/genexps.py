#!/usr/bin/env python

def generator():
    symbols = '£&@#'
    _tuple = tuple(ord(symbol) for symbol in symbols)

    import array
    arr = array.array
    ('I', (ord(symbol) for symbol in symbols))

    return _tuple, arr
